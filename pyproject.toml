[project]
name = "graal"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "duckdb>=1.4.4",
    "fastparquet>=2025.12.0",
    "ipykernel>=7.1.0",
    "ipywidgets>=8.1.8",
    "jupyter-cache>=1.0.1",
    "langchain>=1.2.3",
    "langchain-community>=0.4.1",
    "langchain-neo4j>=0.6.0",
    "langchain-openai>=1.1.4",
    "langchain-text-splitters>=1.1.0",
    "langfuse>=3.11.2",
    "matplotlib>=3.10.8",
    "nbclient>=0.10.4",
    "nbformat>=5.10.4",
    "networkx>=3.6.1",
    "openai-agents>=0.6.3",
    "pacmap>=0.8.0",
    "pandas>=2.3.3",
    "pca>=2.10.1",
    "plotly>=6.5.1",
    "polars>=1.38.1",
    "pyarrow>=23.0.1",
    "s3fs>=2024.12.0",
    "transformers>=4.57.3",
    "umap-learn>=0.5.11",
]

[dependency-groups]
dev = [
    "pre-commit>=4.5.1",
    "ruff>=0.14.9",
]

[tool.ruff]
line-length = 100
extend-include = ["*.ipynb"]
# Cell-based script meant to be run interactively (VS Code / Jupytext), relies
# on IPython magics that are not valid standalone Python syntax.
extend-exclude = ["explorations.py"]
